export const metadata = {
  title: "Log in",
  description: "Log in to your NaijaArtisans account.",
  alternates: { canonical: "/login" },
  robots: { index: false, follow: true },
};

export default function LoginLayout({ children }) {
  return children;
}
